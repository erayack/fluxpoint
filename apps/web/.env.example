# Rust public API base URL (no trailing slash)
FLUXPOINT_RUST_PUBLIC_API_BASE_URL=http://127.0.0.1:3001

# Bearer token for Rust inspector endpoints (optional)
FLUXPOINT_RUST_PUBLIC_API_TOKEN=

# Dashboard access key (optional; if set, browser must send x-fluxpoint-dashboard-key header)
FLUXPOINT_DASHBOARD_KEY=
